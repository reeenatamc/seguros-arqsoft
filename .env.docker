# ============================================
# Configuración para Docker - Producción
# ============================================
# Copia este archivo a .env.production y configura los valores

# Django Configuration
SECRET_KEY=django-insecure-px##4fhfo=x$5b9qz(0!5p&5pm=wn_vycl#2re9zd@rg+d^s)@
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# Database Configuration (PostgreSQL en Docker)
POSTGRES_DB=seguros
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE-THIS-PASSWORD
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:CHANGE-THIS-PASSWORD@db:5432/seguros

# Email Configuration (SMTP para producción)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=seguros@utpl.edu.ec
SERVER_EMAIL=seguros@utpl.edu.ec

# Celery Configuration
CELERY_BROKER_URL=redis://redis:6379/0

# Site Configuration
SITE_URL=http://localhost:8000

# Logging
DJANGO_LOG_LEVEL=INFO

# Gunicorn Configuration
GUNICORN_WORKERS=4
GUNICORN_THREADS=2
GUNICORN_TIMEOUT=120

# Django Superuser (creado automáticamente al iniciar)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=CHANGE-THIS-PASSWORD
DJANGO_SUPERUSER_EMAIL=admin@utpl.edu.ec

# Celery Workers
CELERY_WORKERS=4
CELERY_LOG_LEVEL=info

# Security (opcional en desarrollo, requerido en producción)
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
