# Django Configuration
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (SQLite by default, configure PostgreSQL/MySQL for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/seguros_db

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# For production SMTP:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password
DEFAULT_FROM_EMAIL=seguros@utpl.edu.ec
SERVER_EMAIL=seguros@utpl.edu.ec

# Site Configuration
SITE_URL=http://localhost:8000

# Celery Configuration
CELERY_BROKER_URL=redis://localhost:6379/0

# Logging
DJANGO_LOG_LEVEL=INFO
